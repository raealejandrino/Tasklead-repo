<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tasklead</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
      <header class="hero">
          <div class="flex-row"> 
            <nav class="nav">
              <ul>
                    <li class="li-inline"><a class="link" href="/">Home</a></li>
                    <li class="li-inline"><a class="link" href="/dashboard">Dashboard</a></li>
                    {{#if loggedIn}}
                    <li class="li-inline"><a class="link" href="/" id="logout">Logout</a></li>
                    {{else}}
                    <li class="li-inline"><a class="link" href="/login">Login</a></li>
                    {{/if}}
              </ul>
            </nav>          
          </div>

          <h1 class="app-title"><i class="fab "></i>Tasklead</h1>
      </header>

    <main>
      {{{ body }}}
    </main>
  </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>